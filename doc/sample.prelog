
-[
    def prove if (> (rank (eval env next)) 1) success failure
    if (> next 12) (print "yes") else (print "no")
]-

prove = -[
    process rest {
        def t (next env)
        if (> (rank (eval env t)) 1) success (error "NO_PROOF" t)
    }
]-


^^^ {
    def prove if (> (rank (eval env next)) 1) success failure
    if (> next 12) (print "yes") else (print "no")
}

prove = ^^^ process rest {
    def t (next env)
    if (> (rank (eval env t)) 1) success (error "NO_PROOF" t)
}
